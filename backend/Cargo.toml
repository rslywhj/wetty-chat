[package]
name = "wetty-chat-backend"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1", features = ["full"] }
axum = "0.7"
diesel = { version = "2", features = ["postgres", "r2d2"] }
diesel_migrations = "2"
dotenvy = "0.15"
serde = { version = "1", features = ["derive"] }
